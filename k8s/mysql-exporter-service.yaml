apiVersion: v1
kind: Service
metadata:
  name: mysql-exporter
  namespace: lab-project
  labels:
    app: mysql-exporter
spec:
  ports:
  - port: 9104
    targetPort: 9104
    protocol: TCP
  selector:
    app: mysql-exporter
  type: ClusterIP 